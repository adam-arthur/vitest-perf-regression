{
  "name": "vitest-perf-regression",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest run",
    "test.reporter": "vitest run --reporter dot"
  },
  "devDependencies": {
    "vitest": "^0.26.3"
  }
}